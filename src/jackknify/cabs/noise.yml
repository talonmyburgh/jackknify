cabs:
  noise:
    flavour: python
    command: jackknify.core.noise.noise
    name: noise
    info:
      Calculates a 'noise' cube (std dev) from a folder of FITS files.
    image: ghcr.io/talonmyburgh/jackknify:finalbugfixes
    inputs:
      folder-path:
        info:
          Folder containing input FITS files.
        dtype: Directory
        required: true
        policies:
          positional: true
    outputs:
      out:
        dtype: File
        info:
          The resulting noise cube FITS file.
